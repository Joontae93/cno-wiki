---
const { language, codeBlock } = Astro.props;
export interface Props {
	language: 'js' | 'css' | 'php' | 'html' | 'md' | 'jsx' | 'scss';
	codeBlock: string;
}
---

<pre class={`language-${language}`}>
	<code>{codeBlock}</code>
</pre>
